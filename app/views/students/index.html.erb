<% provide(:title, 'Students') %>
<h1>Students</h1>
<ul class="students">
  <%#= link_to "New Student", student_new_path, class: "btn btn-lg btn-primary" %>
  <%= render 'search' %>
  <br />
  <table class = "table">
    <thead>
      <tr>
        <th><%=sort_link(@q, :id, "id")%></th>
        <th><%=sort_link(@q, :name)%></th>
        <th><%=sort_link(@q, :gender)%></th>
        <th><%=sort_link(@q, :phone_number, "Phone #")%></th>
        <th><%=sort_link(@q, :address, "Address")%></th>
        <th><%=sort_link(@q, :birth_date, "Birth Date")%></th>
        <th><%=sort_link(@q, :created_at)%></th>
        <th><%=sort_link(@q, :updated_at)%></th>
        <th>Departments</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
      </thead>
    <tbody>
        <% @students.each do |student| %>
          <%= render student %>
        <% end %>
    </tbody>
  </table>
  <%= will_paginate %>
</ul>