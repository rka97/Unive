<h3>Course Students</h3> <br />
<%= link_to "Enroll New Student in Course", new_student_course_enrollment_path(:course => @course), class: "btn btn-primary" %>
<table class = "table table-bordered table-responsive">
    <thead>
    <tr>
        <th><%=sort_link(@q, :id)%></th>
        <th><%=sort_link(@q, :name)%></th>
        <% if current_user.admin? %>
            <th>Remove from Course</th>
        <% end %>
    </tr>
    </thead>
    <tbody>
        <% @students.each do |student| %>
            <tr>
                <th scope="row"><%= student.id %></th>
                <td><%= link_to student.name, student %></td>
                <% if current_user.admin? %>
                    <td><%= link_to "Delete", student_course_enrollment_path(id: (student.course_enrollments.find_by(course_id: @course.id)).id), :method => :delete, :data => {confirm: "Are you sure?"} %></td>
                <% end %>
            </tr>
        <% end %>
    </tbody>
</table>
<%= will_paginate @students, :param_name => "students_page" %>